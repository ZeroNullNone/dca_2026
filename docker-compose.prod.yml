services:
  btc-dca:
    build: .
    user: "${APP_UID:-1000}:${APP_GID:-1000}"
    env_file:
      - .env
    ports:
      - "127.0.0.1:${BTC_DCA_HOST_PORT:-8082}:8765"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
